function arrayPosition(arr){
    // Retornar o quinto item do array recebido na function.
   var arr = ["lucas", "pedro", "joão", "matheus", "tobias", "josias", "marcelo"];
   // "var i" vai percorrer o array, e quando ele chegar na posição 5, vai retornar o elemento da posição 5
     for(var i = 0; i < arr.length; i++) {
       if(arr[i] = arr[i = 5]){
         return arr[i = 5];
       }
     }
   }
   //executando a função
   arrayPosition();